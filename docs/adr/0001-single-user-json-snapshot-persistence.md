# Single-user JSON snapshot persistence

NativeNote will persist each Page's canonical editable content as a single-user JSON snapshot for the next 6–12 months, rather than use collaborative persistence or block-per-row storage. This fits the current product scope while preserving a clear migration path if collaboration becomes a real requirement.
