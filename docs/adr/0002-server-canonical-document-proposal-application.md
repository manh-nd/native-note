# Server-canonical DocumentProposal application

NativeNote will apply accepted DocumentProposals on the server against the canonical StoredDocument, rather than trust client-side mutation application. This makes validation, conflict detection, document updates, Finding decisions, and eligible LearningItems one atomic, auditable decision.
