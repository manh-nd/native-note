services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: native_note
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - native_note_pg:/var/lib/postgresql/data

volumes:
  native_note_pg:
